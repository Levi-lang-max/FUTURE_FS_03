[build]
  command = "bun run build"
  publish = "dist/client"

# SPA fallback — TanStack Router handles routing client-side
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
